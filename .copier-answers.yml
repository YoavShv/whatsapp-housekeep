# This file is managed by Copier; DO NOT edit it by hand.
# It records which template version this project was last synced to, so that
# `copier update` can perform a 3-way merge. Commit it with the project.
_commit: v0.1.3
_src_path: git@github.com:mobilegroup-sw/base-proj-ai.git

